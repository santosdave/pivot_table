/**
 * A row dimension label and whether it will be visible.
 */
export interface RowLabelVisibility {
  /**
   * The label to display.
   */
  label: string;

  /**
   * Indicates whether the label will be visible.
   */
  visible: boolean;
}
