/**
 * The titles to display in the title row of the pivot table.
 */
 export interface TitleNames {
    /**
     * The text to display at the top of the table above the column dimensions.
     */
    column?: string;
  
    /**
     * The text to display at the top of the table above the row dimensions.
     */
    row?: string;
  }
  